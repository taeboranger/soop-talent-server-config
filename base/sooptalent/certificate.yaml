apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sooptalent-tls
spec:
  secretName: sooptalent-tls
  issuerRef:
    name: letsencrypt-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - soop-talent.cagea1.com
